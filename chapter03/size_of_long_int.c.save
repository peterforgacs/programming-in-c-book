#include <stdio.h>

void print_int_size(int);

int main(void){
	short int i1;
	int i2;
	unsigned int i3;
	long int i4;
	unsigned long int i5;
	long long int i6;
	unsigned long long int i7;

	printf("short int %hi size is %i bytes which is %i bits.\n", i1, sizeof(i1), sizeof(i1)*8);
printf("Float %g size is %i bytes which is %i bits.\n", f, bytes, bits);
	printf("Float %g size is %i bytes which is %i bits.\n", f, bytes, bits);
	printf("Float %g size is %i bytes which is %i bits.\n", f, bytes, bits);
	printf("Float %g size is %i bytes which is %i bits.\n", f, bytes, bits);
	printf("Float %g size is %i bytes which is %i bits.\n", f, bytes, bits);
	printf("Float %g size is %i bytes which is %i bits.\n", f, bytes, bits);
	printf("Float %g size is %i bytes which is %i bits.\n", f, bytes, bits);
	return 0;
}

void print_float_size (float f){
	// Prints the size of a float in memory
	// @param f float
	int bytes = sizeof(f);
	int bits = bytes * 8;
	printf("Float %g size is %i bytes which is %i bits.\n", f, bytes, bits);
}

